#include <stdio.h>

int main()
{
	//int tv, fv;
	//tv = (1 < 2);	//expression은 값을 가진다.
	//fv = (1 > 2);

	//printf("True is %d\n", tv);
	//printf("False is %d\n", fv);

	//int i = 5;
	//while (i)	//while은 0이면 false이고 나머지 다 true.
	//	printf("%d is true\n", i--);
	//printf("%d is false\n", i);

	while (1)	//항상 true로 고정시킴. 무한 loop를 의도.
	{

	}

	return 0;
}